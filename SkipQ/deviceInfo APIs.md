# Device Information apis

1. `queryDeviceInformation` (REQUEST) - for getting details from the sdk of device and sending it to getDeviceDetails (API)  for getting the authenticated.

2. `alert` (NOTIFICATION) - system notifyies the application for error , which we will be sending via api.